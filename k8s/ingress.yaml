apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mandelbrot
  namespace: default
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mandelbrot
                port:
                  number: 80
